//- 입력받은 문자의 ASCII 코드값 + 2
//- (입력받은 문자의 ASCII 코드값 * 7) % 80 + 48
#include <stdio.h>
int main(){
    char pw[20];
    int i = 0;
    scanf("%s", pw);
    while (i<=20)
    {
        ("%s", pw[i]+2);
        i++;
    }
    return 0;
}